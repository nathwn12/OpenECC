---
description: "Alias for /swarm — execute full engineering pipeline"
agent: swarm-coordinator
---

# Make Command

Alias for /swarm. Resolves to the same pipeline.

See `.opencode/commands/swarm.md` for full documentation.

For: $ARGUMENTS
